#include <iostream>
#include <vector>

std::vector<std::vector<int>> combinations(std::vector<int> vec, int n){
    std::vector<std::vector<int>> l;
    std::vector<int> temp;
    if(n == 0){
        return l;
    }
}