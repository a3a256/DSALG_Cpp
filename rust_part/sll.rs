use std::convert::TryInto;


enum Option{
    Some(T),
    None,
}


struct Node{
    let mut value: Option<i32> = None;
    struct Node next;

    fn Node(val: i32){
        value = Some(value);
    }
}